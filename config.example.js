// ========================================
// Realtrace · 开源版配置示例
// 复制为 config.js 并填入你的端点后使用
// config.js 会被 .gitignore 忽略，永不提交
// ========================================

window.RT_CONFIG = {
  // ── 官方创世链锚定（个人免费）────────────────
  // 指向官方锚定服务后，封章结果会自动上链到官方创世链
  // 自建 / 离线模式：保持 apiBase 为空字符串即可（完全本地，不上传任何数据）
  anchor: {
    apiBase: "",               // 例: "https://rt.example.com" —— 以官方发布为准
    genesisPublicKey: "",      // 官方创世链根公钥（官方渠道公布后填入，用于标识链来源）
    enabled: false             // true = 封章时向官方锚定服务提交链根
  }
};